# Core
db:
  redis:
    emulate: true
    namespace: EG

plugins:
  express-gateway-plugin-cas:
    package: '../dist/manifest.js'
    DATABASE_URL: ${DATABASE_URL:-}
    ADMIN_KEY: ${ADMIN_KEY:-}
    # JWT_EXPIRATION_DELTA: '10s'
    # JWT_REFRESH_EXPIRATION_DELTA: '20s'